liquibase:
  change-log: classpath:/db/changelog/db.changelog-master.yml
server:
  port: 9090
---
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  profiles: dev
---

spring:
  profiles: prod
  jpa:
    hibernate:
      ddl-auto: update